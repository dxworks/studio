import {init, StudioInitOptions} from '../src/commands/init'

describe('init test', () => {
  it('should set all fields for options', () => {
    // // @ts-ignore
    // const options: StudioInitOptions = {chronosDockerRegistry: ''}
    // init('test', options)
    // expect(options.dir).toBeTruthy()
    // expect(options.chronosTag).toBeTruthy()
    // expect(options.chronosPort).toBeTruthy()
    // expect(options.chronos1Tag).toBeTruthy()
    // expect(options.chronos1Port).toBeTruthy()
    // expect(options.chronosDockerRegistry).toBeTruthy()
    // expect(options.network).toBeTruthy()
    // expect(options.inspectorGitTag).toBeFalsy()
    // expect(options.inspectorGitPort).toBeFalsy()
  })
})
